// tests go here; this will not be compiled when this package is used as a library

basic.forever(() => {
    mesh.initRadio()
    mesh.sendText("According to all known laws of aviation,\
 there is no way a bee should be able to fly.")
})
